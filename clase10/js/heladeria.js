//prompt alert
// ask with prompt to introduce the first ice-cream taste and store the data in a variable
let firstTaste = (prompt('Ingrese su 1er gusto: ')).toUpperCase();
// ask with prompt to introduce the second ice-cream taste and store the data in a variable
let secondTaste = (prompt('Ingrese su 2do gusto: ')).toUpperCase();
// ask with prompt to introduce the third ice-cream taste and store the data in a variable
let thirdTaste = (prompt('Ingrese su 3er gusto: ')).toUpperCase();

alert(`Aquí tiene su helado de ${firstTaste}, ${secondTaste} y ${thirdTaste}`)